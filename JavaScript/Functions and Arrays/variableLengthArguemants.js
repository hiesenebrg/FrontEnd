function sum(a ,b){
    return a+b;
}
console.log(sum(4));
// it will nto thore an error instead of this it will give NaN(not a number) because still remains undefined

console.log(sum(4,5,9));
// extra arguement is simply ignored